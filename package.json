{
  "name": "code-pipeline-slack-notifier",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "mocha",
    "dist": "mkdir target ; zip -r target/lambda lib node_modules LICENSE README.md"
  },
  "devDependencies": {
    "mocha": "^3.5.0"
  },
  "dependencies": {
    "request": "^2.83.0"
  }
}
